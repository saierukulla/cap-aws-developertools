we not able to veiw the log file in s3 bucket


elastic bean stalk work flow and cfn signals


cd /etc/ansible ---for config files

password based authentication: ansible all -m ping -u <username of remote machine> -k

ansible all -i /root/sai/inv -m ping -u <username of remote machine> -k


we need to create the genrate the key using ssh-key-gen by using user access and copy public key to remote machines.
key based authentication: ssh-copy-id <private ip of remote machine>